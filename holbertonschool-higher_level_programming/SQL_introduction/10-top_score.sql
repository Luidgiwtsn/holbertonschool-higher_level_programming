-- Ce script liste les scores et les noms de la table 'second_table'.
-- Les résultats sont triés par score, du plus élevé au plus bas (ordre décroissant).
-- La colonne 'score' s'affiche en premier, suivie de la colonne 'name'.
SELECT score, name FROM second_table ORDER BY score DESC;
