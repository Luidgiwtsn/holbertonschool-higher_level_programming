-- Ce script liste toutes les lignes de la table 'first_table' de la base de données actuelle.
-- L'astérisque (*) est un raccourci qui signifie "sélectionner toutes les colonnes".
-- Le résultat affiche les données pour les colonnes 'id' et 'name'.
SELECT * FROM first_table;
